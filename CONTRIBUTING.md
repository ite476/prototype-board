# 기여 안내

프로토타입 보드는 기획 문서와 화면 시안을 확인하기 위한 로컬 단일 사용자용 초안입니다. 일반적인 코드·문서·테스트·가상 예제 개선은 환영하지만, 실제 업무 자료를 저장하거나 특정 조직의 시스템에 연결하는 변경은 이 저장소의 범위에 포함하지 않습니다.

## 개발 환경

- Node.js 22.18 이상
- 의존성 설치: `npm ci`
- 테스트: `npm test`
- 타입 검사와 배포용 화면 빌드: `npm run build`

변경을 제출하기 전에 테스트와 빌드를 실행하고, 결과를 pull request에 적어 주세요.

## 변경할 때 지킬 점

- 로컬 파일 저장과 현재의 단일 사용자 범위를 유지합니다.
- 인증·사용자 권한·외부 API·운영 데이터·DB 도입은 먼저 이슈에서 범위를 논의합니다.
- 실제 개인정보, 조직 자료, 인증정보, 실행 중 생성된 보드 기록, 컴퓨터의 절대 경로를 커밋하지 않습니다.
- 화면 시안은 외부 CDN·폰트·이미지·API에 의존하지 않는 독립 HTML로 유지합니다.
- 동작을 바꾸는 변경에는 관련 테스트와 문서를 함께 수정합니다.

## 이슈와 pull request

작은 버그 수정과 문서 보완은 바로 pull request를 열어도 됩니다. 저장 형식·API·권한 범위를 바꾸는 작업은 먼저 이슈를 열어 문제와 제안하는 변경을 설명해 주세요.

Pull request에는 다음 내용을 포함해 주세요.

1. 해결하려는 문제
2. 변경한 내용
3. 실행한 검증 명령과 결과
4. 이번 변경으로 보장하지 않는 범위

한 pull request에는 서로 밀접한 변경만 담아 리뷰 범위를 작게 유지해 주세요. 등록된 기획이나 화면 시안은 실제 제품 개발·배포가 완료되었다는 뜻이 아닙니다.

## 공개하지 말아야 할 정보

비밀값이나 실제 자료가 실수로 포함되었다면 공개 이슈에 원문을 올리지 말고 저장소 관리자에게 먼저 알려 주세요. 이미 커밋한 인증정보는 사용 중지와 교체가 우선이며, 단순히 파일을 삭제하는 것만으로는 Git 이력에서 제거되지 않을 수 있습니다.

name: prototype-board
services:
  web:
    build: .
    ports:
      - "127.0.0.1:${BOARD_WEB_PORT:-5188}:80"
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://127.0.0.1/ || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 10
